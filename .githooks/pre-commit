#!/bin/sh
# Gate every commit on the unit suite (seconds). Bypass: git commit --no-verify
cd "$(git rev-parse --show-toplevel)" || exit 1
echo "pre-commit: npm test"
npm test || { echo "pre-commit: unit tests failed — commit blocked"; exit 1; }
